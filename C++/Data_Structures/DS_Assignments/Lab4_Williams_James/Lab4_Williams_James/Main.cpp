#include <string>
#include <iostream>
#include <vector>
using namespace std;

int main(){
	vector<string> my_vec;


	//int an_array[] = { 0, 1, 2, 3, 4, 5, 6, 7};

	//for (int i = 0; i < 7; i++)
	//{
	////	an_array[i + 1] = an_array[i];
	////	cout << an_array[i] << endl;
	//}

	//int an_array[] = { 0, 1, 2, 3, 4, 5, 6, 7 }; // initialize array of integers containing the specified values
	//for (int i = 7; i > 3; i--){                 // set i to 7 then loop through by decramenting, then don't loop index 3
	//	an_array[i] = an_array[i - 1];			 // new array values {0,1,2,3,3,4,5,6}
	//}

	system("pause");
	return 0;
}